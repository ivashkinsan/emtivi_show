import React from 'react';
import { ModelViewer } from './components/ModelViewer';

export const Test3DChannel: React.FC = () => {
    return (
        <div style={{ width: '100%', height: '100%' }}>
            <ModelViewer />
        </div>
    );
};

export default Test3DChannel;
