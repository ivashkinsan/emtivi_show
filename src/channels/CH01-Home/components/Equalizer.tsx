import Loader from '../../../components/atoms/Loader/Loader';

export const Equalizer = () => {
  // Reuse the Loader component for a decorative equalizer effect
  return <Loader variant="equalizer" size="sm" />;
};
