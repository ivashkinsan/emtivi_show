import React from 'react';

// Placeholder for future theme logic (e.g., light/dark mode)
export const ThemeProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};
